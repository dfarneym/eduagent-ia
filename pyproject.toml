[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "eduagent-ia"
version = "0.1.0"
description = "Educational RAG application built with LangChain"
requires-python = ">=3.11"

[tool.setuptools.packages.find]
where = ["src"]